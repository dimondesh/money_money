export const selectIsModalAddTransactionOpen = (state) =>
  state.modal.isModalAddTransactionOpen;
export const selectIsModalEditTransactionOpen = (state) =>
  state.modal.isModalEditTransactionOpen;
export const selectEditTransactionId = (state) => state.modal.editTransactionId;

export const selectIsLogOutModalOpen = (state) => state.modal.isLogOutModalOpen;
